// Configuration file for nanofetch
// Edit these values to customize nanofetch behavior

// Display ASCII logo (set to false to disable)
pub const show_logo = true;

// Minimum terminal width (columns) to show logo
// Logo will be hidden if terminal is narrower than this
pub const min_width_for_logo: usize = 70;

// Logo selection: "auto" to detect OS, or specific name like "NixOS", "Arch", "Ubuntu"
pub const logo = "auto";
